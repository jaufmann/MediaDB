import { LengthtrimmerPipe } from './lengthtrimmer.pipe';

describe('LengthtrimmerPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthtrimmerPipe();
    expect(pipe).toBeTruthy();
  });
});
